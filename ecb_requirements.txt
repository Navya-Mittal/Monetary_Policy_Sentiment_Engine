numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
yfinance>=0.2          # optional — only needed for live market data
requests>=2.31         # optional — only needed for live ECB speech scraping
beautifulsoup4>=4.12   # optional — only needed for live ECB speech scraping
